import { StyleSheet } from 'react-native';

const default_styles = StyleSheet.create({
    // Base text for all texts
    baseText: {
        fontFamily: 'Jaldi',
    },
    // Bold text for all texts
    boldText: {
        fontFamily: 'Jaldi-bold'
    },
});

export default default_styles;