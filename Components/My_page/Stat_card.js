import { View } from "react-native";

const Stat_card = () => {
    return (
        <View
            style={{
                width: "100%",
                height: 100,
                backgroundColor: "#FFFAE0",
                borderRadius: 10,
            }}
        ></View>
    );
};

export default Stat_card;
